// Minimal placeholder for particles.js
// The real implementation can be dropped in later. This prevents 404 errors
// and allows the page to load when served.
(function () {
  // No-op initialization; if a real particles system exists it can hook here.
  if (typeof window !== 'undefined') {
    window.particlesInit = window.particlesInit || function () {
      // placeholder
      return;
    };
  }
})();
